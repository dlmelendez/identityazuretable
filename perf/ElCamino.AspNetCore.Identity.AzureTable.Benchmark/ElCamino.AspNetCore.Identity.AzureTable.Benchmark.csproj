<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>14.0</LangVersion>
    <UseLocalProject Condition="'$(UseLocalProject)' == ''">true</UseLocalProject>
    <BenchmarkPackageVersion Condition="'$(BenchmarkPackageVersion)' == ''">10.0.*</BenchmarkPackageVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BenchmarkDotNet" Version="0.14.0" />
  </ItemGroup>

  <ItemGroup Condition="'$(UseLocalProject)' == 'true'">
    <ProjectReference Include="..\..\src\ElCamino.AspNetCore.Identity.AzureTable\ElCamino.AspNetCore.Identity.AzureTable.csproj" />
  </ItemGroup>

  <ItemGroup Condition="'$(UseLocalProject)' != 'true'">
    <PackageReference Include="ElCamino.AspNetCore.Identity.AzureTable" Version="$(BenchmarkPackageVersion)" />
  </ItemGroup>

</Project>
